/* STUB (Agent A owns the real root layout) — pass-through so [locale]/layout owns <html>. */
import type { ReactNode } from "react";
import "./globals.css";

export default function RootLayout({ children }: { children: ReactNode }) {
  return children;
}
