import type { ExperienceItem } from './types';

/** Order of the experience timeline. All copy lives in
 * `messages/*.json` under `experience.items.<key>`. */
export const experience: ExperienceItem[] = [
  { key: 'learntech' },
  { key: 'goskillup' },
  { key: 'degree' },
];
